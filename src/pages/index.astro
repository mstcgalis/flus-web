---
import Prose from "../components/Prose.astro";
import { getEntry } from "astro:content";

import Base from "../layouts/Base.astro";

const entry = await getEntry("articles", "about");
const { Content } = await entry.render();
---

<Base title="flus.fm">
  <Prose>
    <Content />
  </Prose>
</Base>
